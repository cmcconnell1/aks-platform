# Terraform Backend Configuration for Staging Environment
# NOTE: Update storage_account_name with your actual staging storage account name
# Run: ./scripts/setup-azure-credentials.sh --project-name aks-platform --environment staging
# to create the backend resources automatically

resource_group_name  = "aks-platform-terraform-state-rg"
storage_account_name = "aksplatftfstaging000000"
container_name       = "tfstate"
key                  = "staging/terraform.tfstate"
