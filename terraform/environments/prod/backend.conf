# Terraform Backend Configuration for Production Environment
# NOTE: Update storage_account_name with your actual production storage account name
# Run: ./scripts/setup-azure-credentials.sh --project-name aks-platform --environment prod
# to create the backend resources automatically

resource_group_name  = "aks-platform-terraform-state-rg"
storage_account_name = "aksplatftfprod000000"
container_name       = "tfstate"
key                  = "prod/terraform.tfstate"
